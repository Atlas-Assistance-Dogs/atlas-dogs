- include_file: common.recipe.yml
- include_file: contact.recipe.yml

- object: Program_Assignment__c
  fields:
      Assigned_Date__c:
          date_between:
              start_date: 2000-01-01
              end_date: today
      Completion_Date__c:
          date_between:
              start_date: ${{date(Assigned_Date__c)}}
              end_date: today
      Contact__c:
          reference: Person
      Program__c: ${{Contact__c.Position__c}}
      Status__c:
          random_choice:
              - In progress
              - Completed
              - Discontinued
              - Removed
